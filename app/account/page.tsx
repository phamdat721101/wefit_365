import Account from "@/components/Account";

export default function AccountPage() {
    return (
        <main className="flex flex-col min-h-screen bg-white text-black p-4">
            <Account />
        </main>
    );
}